{
  "name": "afp-dashboard",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync": "bash ../scripts/sync-dashboard-brand.sh",
    "predev": "npm run sync",
    "dev": "astro dev",
    "prebuild": "npm run sync",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^6.4.6"
  }
}
